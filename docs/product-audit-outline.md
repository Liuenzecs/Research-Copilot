# Research Copilot 产品审计与迭代纲要

本文档用于记录当前仓库的产品定位、阶段判断、已完成批次与暂缓事项。

## 产品定位

- 本地优先
- 单用户研究工作台
- 面向真实周节奏科研使用
- 核心不是“通用聊天”，而是“论文—复现—心得—周报—记忆”的连续工作流

## 当前阶段判断

当前项目已经进入“可日常使用的单人研究工作台”阶段：

- 独立论文阅读页已成为新的论文主入口，`Paper Workspace` 作为其中的业务工作面继续复用
- `paper -> summary -> reflection -> memory` 主链路可用
- `paper -> repo candidates -> reproduction` 已经闭环
- `reproduction` 已具备计划、步骤、日志、阻塞与心得回流
- `weekly report` 已具备严格按周上下文、历史快照与精确回跳
- `memory` 已具备精确回跳与列表内解释

当前不再优先扩展大功能面，而是继续保持：

- 工作流闭环清晰
- Chinese-first 体验一致
- 页面状态提示自然
- 回跳链路稳定

## 已完成批次

### 第一批：论文工作区到复现工作区的闭环修顺

完成日期：2026-03-15

已完成：

- 修正 `Paper Workspace` 的摘要语义
- 支持真正的“当前查看摘要 / 不绑定摘要”
- 从论文工作区进入 `/reproduction?paper_id=<id>`
- 复现页自动搜索 repo candidates
- 自动续接该论文最近一条 reproduction
- 新增 `GET /reproduction`
- `POST /repos/find` 改为应用层复用，避免重复 repo 记录

### 第二批：复现工作区细化 + blocker/log 正式接入

完成日期：2026-03-15

已完成：

- 复现页顶部上下文状态区补齐
- `ReproStepTracker` 升级为步骤卡片视图
- 正式接入步骤级 `reproduction_logs`
- 支持 `note / blocker` 两类日志
- blocker 日志自动将步骤切到 `blocked`
- 本地 `log_analyzer` 输出 `error_type / next_step_suggestion`
- 复现页 warning / empty / notice 文案收口

### 第三批：周报上下文修整 + 周报回跳闭环

完成日期：2026-03-15

已完成：

- `weekly report context` 改为严格按周过滤
- 周报左侧面板补齐五段内容
- paper / reproduction 条目支持精确回跳
- 历史草稿打开时使用 `source_snapshot_json`
- 周报 markdown 与左侧上下文对齐
- reproduction 页面剩余提示与 reflection 回流文案收口

### 第四批：Memory 精确回跳 + 页面状态统一 + Chinese-first 收敛

完成日期：2026-03-15

已完成：

- `memory/query` 返回 `jump_target`
- memory 结果支持精确回跳到 `paper / reproduction / reflection / brainstorm`
- `SearchPage` 支持 `paper_id + summary_id`
- `ReflectionsPage` 支持 `reflection_id`
- 新增统一状态组件 `StatusBanner / StatusStack`
- 多个主页面统一 notice / warning / error 呈现
- memory 页面与部分表单继续收口为 Chinese-first
- 页面零散 `fetch` 收回到 `frontend/src/lib/api.ts`

### 第五批：周报编辑收口 + 周节奏时间线 + Memory 可解释性补齐

完成日期：2026-03-15

已完成：

- 周报页支持“当前周已有草稿”时的分流选择
  - 继续最近草稿
  - 新建一份草稿
  - 取消
- 新增最小 `ChoiceDialog` 组件
- `ReportDraftEditor` 支持：
  - 中文状态显示
  - 草稿元信息展示
  - dirty state 检测
  - “恢复到已保存版本”
- 历史草稿列表支持：
  - 当前草稿高亮
  - 本周草稿标记
  - 周范围 / 更新时间展示
- `GET /reflections` 新增 `is_report_worthy` 筛选
- `reflections` 页面改为周节奏优先：
  - 默认本周
  - 今天 / 昨天 / 本周 / 上周 / 最近30天 / 全部
  - 心得类型 / 生命周期 / 仅可汇报筛选
  - 顶部 summary 行
- `memory` 列表新增：
  - `retrieval_mode`
  - `match_reason`
  - `context_hint`
- `memory` 列表改为“类型 + 层级 + 摘要 + 解释 + 回跳”
- `dashboard` 不再直接暴露 raw `task_type / status`
- 新增共享 presentation helper，统一中文标签与时间展示

### 第六次稳定性修补：首屏 API 不可达兜底

完成日期：2026-03-16

已完成：

- `dashboard` 首屏加载失败时改为页面内错误提示，不再抛出未捕获运行时异常
- `library` 首屏加载失败时改为页面内错误提示
- `weekly-report` 历史草稿首屏加载失败时改为页面内错误提示
- `frontend/src/lib/api.ts` 将原始 `Failed to fetch` 收口为更明确的中文网络错误说明

### 第七次界面收口：独立论文阅读页 + 顶栏导航化 + 选词翻译

完成日期：2026-03-16

已完成：

- 左侧边栏与右侧上下文面板移除，主导航统一迁到顶部 `Topbar`
- 顶栏新增“功能说明”抽屉，使用用户向说明内容介绍主工作流
- `/search` 改为纯论文搜索页，不再内嵌右半屏 `Paper Workspace`
- 新增 `/papers/[paperId]` 独立论文阅读页，作为新的论文主入口
- 保留旧深链兼容：访问 `/search?paper_id=...&summary_id=...` 会自动跳到新阅读页
- 阅读页默认展示本地 PDF 解析正文，支持更舒适的全宽阅读排版
- 解析正文支持选词翻译，优先走 LibreTranslate 兼容公共接口，失败时回退到本地辅助结果
- `PaperWorkspace` 改为可嵌入的独立业务面，在阅读页中作为次级工作区复用
- 文献库入口与结果卡片改为指向独立阅读页，并修正文案与操作区布局
- 设置页补充公共翻译接口的只读配置状态展示

### 第八次稳定性修补：长期记忆持久化路径锁定 + 最近记忆可见性

完成日期：2026-03-16

已完成：

- 运行时数据路径新增对历史 `backend/backend/data` 漂移路径的自动收口，统一落到规范 `backend/data`
- 增加 `GET /memory` 最近记忆列表接口，避免只有检索入口时误以为“没有长期记忆”
- `memory` 页面默认展示最近写入的长期记忆
- 空查询时会回到最近记忆视图，不再只提示用户必须先检索
- 为路径规范化与最近记忆列表补充后端测试

### 第九次交互收口：设置页运行路径可见化 + 去 ID 化入口

完成日期：2026-03-16

已完成：

- 设置页显示当前实际运行的数据库 URL、数据库文件路径、数据目录与向量目录
- 设置页提示历史 `backend/backend/data` 目录已被自动收口到 `backend/data`
- 复现页不再要求手工输入 `paper_id / reproduction_id`
- 复现页改为“按论文标题搜索进入”与“最近复现记录”两种更自然入口
- 反思卡片将显眼的 raw ID 标签改为更自然的动作型文案

## 当前结论

本轮“三次做完剩余代办”的主目标已全部完成。

当前仓库更适合继续做：

- 小范围稳定性修补
- 真实使用后的交互微调
- 少量对象解释性增强
- 阅读页细节与阅读体验继续打磨

不建议现在立即进入：

- memory graph 真图谱化
- profile 面板扩展
- 新增独立工作面
- 大范围架构重写

## 暂缓项

以下内容继续列为 `Defer`：

- memory graph 真图谱化
- researcher profile 面板
- repo 独立工作面
- task 独立工作面
- 更大范围的 hooks / store / 路由重构

## 维护规则

每次完成一批计划后：

1. 更新“已完成批次”
2. 删去已完成的待办
3. 仅保留真实仍未完成的 defer 项
4. 保持文档与仓库代码状态一致

## 第十次交互收口：减少 raw ID 暴露 + 入口进一步人性化

完成日期：2026-03-16

已完成：

- `dashboard` 中反思与任务列表不再把 `#id` 作为主文案显示
- `reproduction` 页继续收口上下文文案，不再要求用户理解 `paper_id / reproduction_id`
- `PaperWorkspace` 的摘要上下文与论文心得列表移除明显的 ID 暴露
- `WeeklyReportPanel` 的论文跳转统一指向独立阅读页 `/papers/[paperId]`
- `ReproStepTracker` 的编辑与日志入口标题改为自然中文
- `reproduction / reporting / paper workspace` 中残余的 `paper-only` 文案改为“仅论文上下文”
- 复现步骤状态下拉选项改为中文展示：待开始 / 进行中 / 已完成 / 已阻塞

本轮结论：

- 当前高频主链路已经基本不再要求用户记住内部对象 ID
- 用户看到的主要入口已更接近“按论文标题、论文上下文、最近记录继续工作”的真实使用方式
- 后续若继续优化，应优先做对象标题补全、历史切换与阅读微交互，而不是重新引入新的编号式入口

## 第十一次阅读体验收口：正文段落定位与高亮

完成日期：2026-03-16

已完成：

- 独立论文阅读页支持按关键词定位正文段落
- 正文段落支持点击高亮，并把当前位置同步到阅读页 URL
- 新增“上一段 / 下一段”快速切换，适合连续精读
- 选词翻译完成后会自动回到对应段落，减少上下文丢失
- 阅读页新增段落状态提示：总段数、当前段落、关键词命中数
- `paperReaderPath` 支持可选 `paragraph_id` 深链

本轮结论：

- 阅读页已经从“只能滚动浏览正文”提升到“可以定位、回看、继续读”的状态
- 这类微交互比继续扩展新面板更能提升真实日常阅读效率

## 第十二次阅读体验收口：段落批注与批注回看

完成日期：2026-03-16

已完成：

- 新增 `paper_annotations` 持久化表，批注可跨重启保留
- `GET /papers/{paper_id}/reader` 现在会返回正文批注列表
- 新增 `POST /papers/{paper_id}/annotations`，支持按段落写入批注
- 阅读页支持对当前高亮段落直接写批注
- 若用户先选中文字再写批注，会同时保留引用原文
- 阅读页新增“批注回看”面板，点击批注可跳回对应正文段落

本轮结论：

- 阅读工作流已经从“阅读 + 翻译”扩展到“阅读 + 标记 + 回看”
- 这比继续扩展新的独立页面更贴近真实单人科研阅读习惯

## 第十三次阅读体验收口：正文重建 + 页面预览 + 近正文图片插入

完成日期：2026-03-16

已完成：

- `GET /papers/{paper_id}/reader` 升级为结构化阅读数据，正文段落新增 `page_no`
- 阅读接口新增 `pages / figures / reader_notices`
- 后端按 PDF block 重建正文，较当前纯文本抽取更适合双栏论文阅读
- 新增本地缓存的页面预览与 figure 提取能力
- 页面预览与 figure 均通过后端受控 URL 暴露，不暴露本地绝对路径
- 阅读页新增横向页面缩略图条，可同步当前正文页并跳转
- figure 按 `anchor_paragraph_id` 近正文插入；无法稳定锚定时落入补充图片区
- 阅读页新增图片放大预览层，便于查看原页面与图像细节

本轮结论：

- 阅读页已从“纯文本替代视图”提升为“正文主视图 + 原页面辅助 + 图像回看”的更完整工作面
- 这轮改动比继续堆新功能更直接提升真实论文精读体验

## 第十四次阅读体验收口：按页阅读 + 翻译纠偏与加速

完成日期：2026-03-16

已完成：

- 阅读页从整篇长卷模式切换为按页阅读模式
- 新增上一页 / 下一页 / 跳页选择，当前阅读节奏更接近真实翻页
- 当前页正文改为连续排版，不再把每一段都渲染成独立卡片
- 当前页仍保留批注、图像、页面预览和选词翻译
- 选词翻译固定走英译中校验，识别并拦截“英译英”无效结果
- 选词翻译新增缓存复用，相同选中内容可快速返回
- 选词翻译失败时直接回落到本地中文辅助结果，不再等待慢模型

本轮结论：

- 阅读体验已从“可看”收敛到“更像真正翻论文”
- 对单人研究使用而言，翻页节奏与翻译响应速度比继续扩展更多面板更重要

## 第十五次收口：DeepSeek 流式翻译与流式总结

完成日期：2026-03-16

已完成：

- 选词翻译主链路切换为 `DeepSeek` 优先，不再默认依赖公共免费翻译接口
- 新增 `POST /translation/segment/stream`，阅读页可实时看到中文翻译逐段输出
- 选词翻译保留缓存复用，同一句再次翻译时优先返回历史结果
- 新增 `POST /summaries/quick/stream` 与 `POST /summaries/deep/stream`
- `PaperWorkspace` 中“快速总结 / 深度总结”改为流式生成，完成后自动保存为正式摘要
- 强化翻译 prompt，要求只输出自然中文，避免英译英回显
- 模型不可用时仍返回中文辅助结果，不阻断论文阅读主链路

本轮结论：

- 高频阅读链路里的“选词翻译”和“摘要生成”都从等待式黑箱请求收口成了可见的实时输出
- 对当前单人研究场景而言，复用现有 DeepSeek provider 比继续依赖不稳定公共接口更稳、更符合已有总结体系

## 第十六次收口：阅读主链路重做

完成日期：2026-03-17

已完成：

- 论文搜索链路收口为 `arXiv only`
- 顶栏主入口改为 `阅读`，原“搜索论文”文字导航移除，改为右上角搜索图标
- `/library` 收口成“阅读入口 / 我的文献库”，默认优先展示已下载、已入记忆、已有摘要/心得/复现的论文
- 文献卡片补齐 `已下载 / 已入记忆 / 有摘要 / 有心得 / 有复现 / 核心论文` 等状态标签
- 文献库支持按标题、作者、来源、年份做更人性化的本地过滤
- 阅读页新增“阅读视图 / 论文工作区”切换，不再默认把工作区长内容直接铺在正文下方
- 选词翻译改为底部抽屉显示，避免把翻译卡片插到正文上方
- 图片改为“本页图集”面板查看，放大预览尺寸进一步增大
- 帮助抽屉与设置页文案同步收口到“阅读优先”的产品表达

本轮结论：

- 当前产品的主心智已从“搜索入口”切换为“阅读入口”
- 对真实周节奏研究使用而言，文献库可继续阅读、阅读器不打断正文，比继续加功能更重要
