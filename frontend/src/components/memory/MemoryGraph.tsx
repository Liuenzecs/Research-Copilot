export default function MemoryGraph() {
  return (
    <div className="memory-placeholder-card">
      <h3 className="title" style={{ fontSize: 16 }}>记忆关联图</h3>
      <p className="subtle">当前版本先以可检索列表和上下文回跳为主。图谱视图会作为后续增强，不再占用首屏主注意力。</p>
    </div>
  );
}
